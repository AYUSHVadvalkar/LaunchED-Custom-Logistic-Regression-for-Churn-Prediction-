# Dependencies for Streamlit Community Cloud or similar Python deployments.
# `tensorflow-cpu` supplies the standard `tensorflow` import without GPU packages.
streamlit==1.48.0
tensorflow-cpu==2.20.0
keras==3.15.1
